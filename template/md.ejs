# HTMLS

该目录是一些HTML集合, 包含了平时常见需求的简单实现, 可以打开控制台查看代码;

## 预览链接
<% for( let index=0; index < list.length; index++ ) { %>
- [<%= list[index].slice(0, list[index].indexOf('.')) %>](https://lorainwings.github.io/demos/code-segment/<%=
      list[index].slice(0, list[index].indexOf('.')) %>.md))<% } %>