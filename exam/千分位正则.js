// 将数字千分位格式化为,

const n = BigInt(1341234124312341243131)
const o = { a: 1, b: 2, c: 3 }

const res = `${n}`.replace(/\B(?=(\d{3})+$)/g, ',')

/**
 *
 *  \b是指单词边界，\B是指非单词边界
 *  \b即\w和\W之间的间隙
 *  \B即\w和\w之间的间隙，或\W和\W之间的间隙
 *
 **/
